# Local model defaults used by the demo CLI/UI.
OLLAMA_HOST=http://localhost:11434
OLLAMA_CHAT_MODEL=llama3.1:8b
OLLAMA_EMBED_MODEL=nomic-embed-text

# Optional: install with `uv sync --extra lightrag --extra dev`
# to enable embedded LightRAG mode.
LIGHTRAG_WORKING_DIR=outputs/lightrag
